class AppConstants {
  // WebSocket server URL
  // For local development: 'ws://localhost:8080'
  // For production: Replace with your production WebSocket server URL
  static const String websocketUrl = 'wss://echo.websocket.org';
  static const String apiBaseUrl = 'https://mocki.io/v1/';
  static int? selectedUserId;
}

